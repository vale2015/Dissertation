"use client";
import {Suspense,useState} from "react";import {useSearchParams} from "next/navigation";import Sidebar from "@/components/layout/sidebar";import Topbar from "@/components/layout/topbar";import useManagementReport from "@/hooks/useManagementReport";import ReportToolbar from "@/components/reports/report-toolbar";import ReportSummary from "@/components/reports/report-summary";import ReportFinancials from "@/components/reports/report-financials";import ReportBookingMix from "@/components/reports/report-booking-mix";import ReportDailyTable from "@/components/reports/report-daily-table";import ReportInsights from "@/components/reports/report-insights";import ReportWarnings from "@/components/reports/report-warnings";import {formatDate} from "@/lib/report-formatters";
function ReportContent(){const params=useSearchParams();const selectedDate=params.get("date")||"";const [daysAhead,setDaysAhead]=useState(7);const {report,isLoading,error,refetch}=useManagementReport(selectedDate,daysAhead);return <><section className="dashboard-hero"><h1 className="dashboard-title">Management Reports</h1><p className="dashboard-text">Review operational forecasts and download matching PDF or CSV reports.</p></section><ReportToolbar selectedDate={selectedDate} daysAhead={daysAhead} onDaysChange={setDaysAhead} disabled={isLoading||!report||!!error} onRefresh={refetch}/><div aria-live="polite">{isLoading&&<section className="report-state">Generating management report…</section>}{error&&!isLoading&&<section className="report-state report-error" role="alert"><h2>Report unavailable</h2><p>{error}</p></section>}</div>{report&&!isLoading&&<div className="management-report"><p className="report-period"><strong>Selected date:</strong> {formatDate(report.metadata.selected_date)} · <strong>Forecast period:</strong> {formatDate(report.metadata.start_date)} to {formatDate(report.metadata.end_date)}</p><ReportSummary report={report}/><ReportFinancials report={report}/><ReportBookingMix reference={report.booking_mix_reference}/><ReportDailyTable report={report}/><ReportInsights items={report.operational_insights}/><ReportWarnings warnings={report.warnings}/></div>}</>}
export default function ReportsPage(){return <div className="dashboard-app"><Topbar/><div className="dashboard-shell"><Sidebar/><div className="dashboard-main-area"><main className="dashboard-page"><div className="dashboard-container"><Suspense fallback={<p className="report-state">Loading report controls…</p>}><ReportContent/></Suspense></div></main></div></div></div>}
