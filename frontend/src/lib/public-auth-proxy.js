import{NextResponse}from"next/server";
export async function forwardPublicAccountRequest(request,path){const backend=process.env.BACKEND_API_URL?.replace(/\/$/,"");if(!backend)return NextResponse.json({error:{message:"Account service is not configured."}},{status:500});const origin=request.headers.get("origin");if(origin&&origin!==new URL(request.url).origin)return NextResponse.json({error:{message:"Invalid request origin."}},{status:403});if(!request.headers.get("content-type")?.toLowerCase().startsWith("application/json"))return NextResponse.json({error:{message:"JSON content is required."}},{status:415});try{const body=await request.text();const response=await fetch(`${backend}/${path}`,{method:"POST",headers:{"Content-Type":"application/json","Accept":"application/json"},body,cache:"no-store"});return new NextResponse(response.body,{status:response.status,headers:{"Content-Type":response.headers.get("content-type")||"application/json","Cache-Control":"no-store"}})}catch{return NextResponse.json({error:{message:"Account service is temporarily unavailable."}},{status:503,headers:{"Cache-Control":"no-store"}})}}
