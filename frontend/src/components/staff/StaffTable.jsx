import StaffActionsMenu from"./StaffActionsMenu";export default function StaffTable({staff,...actions}){if(!staff.length)return <p>No staff accounts match these filters.</p>;return <div className="staff-table-scroll"><table className="staff-table"><thead><tr><th>Name</th><th>Application role</th><th>Operational role</th><th>Status</th><th>Last login</th><th>Actions</th></tr></thead><tbody>{staff.map(m=><tr key={m.id}><th><span>{m.full_name}</span><small>{m.email}</small></th><td>{m.role}</td><td>{m.staff_role_name?`${m.department} · ${m.staff_role_name}`:"Not assigned"}</td><td><span className={`staff-status status-${m.status}`}>{m.status}</span></td><td>{m.last_login_at?new Date(m.last_login_at).toLocaleString("en-GB"):"Never"}</td><td><StaffActionsMenu member={m}{...actions}/></td></tr>)}</tbody></table></div>}
