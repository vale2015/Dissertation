"use client";import{useEffect,useRef}from"react";export default function ActivationLinkDialog({result,onClose}){const closeRef=useRef(null);useEffect(()=>{closeRef.current?.focus();const key=e=>{if(e.key==="Escape")onClose()};document.addEventListener("keydown",key);return()=>document.removeEventListener("keydown",key)},[onClose]);return <div className="staff-modal-backdrop"><section className="staff-modal" role="dialog" aria-modal="true" aria-labelledby="link-title"><h2 id="link-title">Secure link created</h2><p>This link is displayed only once. Copy it now.</p><label>Activation or reset link<textarea readOnly value={result.activation_link||result.reset_link}/></label><button type="button" onClick={()=>navigator.clipboard.writeText(result.activation_link||result.reset_link)}>Copy link</button><p>Expires: {new Date(result.expires_at).toLocaleString("en-GB")}</p><button ref={closeRef} type="button" onClick={onClose}>Close</button></section></div>}
