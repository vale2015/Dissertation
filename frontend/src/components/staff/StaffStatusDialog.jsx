export default function StaffStatusDialog({member,status,onConfirm,onClose}){return <div className="staff-modal-backdrop"><section className="staff-modal" role="alertdialog" aria-modal="true"><h2>Confirm account change</h2><p>Change {member.full_name} from {member.status} to {status}? Suspension blocks access immediately. Deactivation is retained for audit purposes.</p><div className="staff-modal-actions"><button onClick={onClose}>Cancel</button><button onClick={onConfirm}>Confirm {status}</button></div></section></div>}
