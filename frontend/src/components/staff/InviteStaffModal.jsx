"use client";import{useState}from"react";export default function RegisterStaffModal({roles,onSubmit,onClose}){const[form,setForm]=useState({full_name:"",email:"",password:"",password_confirmation:"",application_role:"staff",staff_role_id:""}),[error,setError]=useState(""),[busy,setBusy]=useState(false),[show,setShow]=useState(false);async function submit(e){e.preventDefault();setBusy(true);setError("");try{await onSubmit(form)}catch(err){setError(err.message)}finally{setBusy(false)}}return <div className="staff-modal-backdrop"><section className="staff-modal" role="dialog" aria-modal="true" aria-labelledby="register-title"><h2 id="register-title">Register staff member</h2><p>Create an active account. Give the initial password to the staff member securely.</p><form onSubmit={submit}><label>Full name<input required value={form.full_name} onChange={e=>setForm({...form,full_name:e.target.value})}/></label><label>Email<input type="email" autoComplete="off" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label>Initial password<input type={show?"text":"password"} autoComplete="new-password" minLength={12} maxLength={128} required value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label><label>Confirm password<input type={show?"text":"password"} autoComplete="new-password" minLength={12} maxLength={128} required value={form.password_confirmation} onChange={e=>setForm({...form,password_confirmation:e.target.value})}/></label><label><input type="checkbox" checked={show} onChange={e=>setShow(e.target.checked)}/> Show passwords</label><p>Use 12 to 128 characters.</p><label>Application role<select value={form.application_role} onChange={e=>setForm({...form,application_role:e.target.value})}>{["staff","supervisor","manager"].map(x=><option key={x}>{x}</option>)}</select></label><label>Operational role<select value={form.staff_role_id} onChange={e=>setForm({...form,staff_role_id:e.target.value})}><option value="">Not assigned</option>{roles.map(r=><option value={r.id} key={r.id}>{r.department} · {r.role_name}</option>)}</select></label>{error&&<p role="alert">{error}</p>}<div className="staff-modal-actions"><button type="button" onClick={onClose}>Cancel</button><button disabled={busy}>{busy?"Registering…":"Register staff member"}</button></div></form></section></div>}
