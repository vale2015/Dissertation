"use client";import{useState}from"react";export default function InviteStaffModal({roles,onSubmit,onClose}){const[form,setForm]=useState({full_name:"",email:"",application_role:"staff",staff_role_id:""}),[error,setError]=useState(""),[busy,setBusy]=useState(false);async function submit(e){e.preventDefault();setBusy(true);try{await onSubmit(form)}catch(err){setError(err.message)}finally{setBusy(false)}}return <div className="staff-modal-backdrop"><section className="staff-modal" role="dialog" aria-modal="true" aria-labelledby="invite-title"><h2 id="invite-title">Invite staff member</h2><p>Application roles control access. Operational roles describe the restaurant job.</p><form onSubmit={submit}><label>Full name<input required value={form.full_name} onChange={e=>setForm({...form,full_name:e.target.value})}/></label><label>Email<input type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label>Application role<select value={form.application_role} onChange={e=>setForm({...form,application_role:e.target.value})}>{["staff","supervisor","manager"].map(x=><option key={x}>{x}</option>)}</select></label><label>Operational role<select value={form.staff_role_id} onChange={e=>setForm({...form,staff_role_id:e.target.value})}><option value="">Not assigned</option>{roles.map(r=><option value={r.id} key={r.id}>{r.department} · {r.role_name}</option>)}</select></label>{error&&<p role="alert">{error}</p>}<div className="staff-modal-actions"><button type="button" onClick={onClose}>Cancel</button><button disabled={busy}>{busy?"Creating…":"Create invitation"}</button></div></form></section></div>}
