export default function StaffActionsMenu({member,onEdit,onStatus,onReset}){return <div className="staff-actions"><button onClick={()=>onEdit(member)}>Edit</button>{member.status==="active"&&<><button onClick={()=>onStatus(member,"suspended")}>Suspend</button><button onClick={()=>onStatus(member,"inactive")}>Deactivate</button><button onClick={()=>onReset(member)}>Reset password</button></>}{member.status==="suspended"&&<><button onClick={()=>onStatus(member,"active")}>Reactivate</button><button onClick={()=>onStatus(member,"inactive")}>Deactivate</button></>}{member.status==="inactive"&&<button onClick={()=>onStatus(member,"active")}>Reactivate</button>}</div>}
