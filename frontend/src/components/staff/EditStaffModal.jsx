"use client";import{useState}from"react";export default function EditStaffModal({member,roles,onSubmit,onClose}){const[form,setForm]=useState({full_name:member.full_name,application_role:member.role,staff_role_id:member.staff_role_id||""});return <div className="staff-modal-backdrop"><section className="staff-modal" role="dialog" aria-modal="true"><h2>Edit {member.full_name}</h2><form onSubmit={e=>{e.preventDefault();onSubmit(form)}}><label>Full name<input value={form.full_name} onChange={e=>setForm({...form,full_name:e.target.value})}/></label><label>Application role<select value={form.application_role} onChange={e=>setForm({...form,application_role:e.target.value})}>{["staff","supervisor","manager"].map(x=><option key={x}>{x}</option>)}</select></label><label>Operational role<select value={form.staff_role_id} onChange={e=>setForm({...form,staff_role_id:e.target.value})}><option value="">Not assigned</option>{roles.map(r=><option value={r.id} key={r.id}>{r.department} · {r.role_name}</option>)}</select></label><div className="staff-modal-actions"><button type="button" onClick={onClose}>Cancel</button><button>Save</button></div></form></section></div>}
