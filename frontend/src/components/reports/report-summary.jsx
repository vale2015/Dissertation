import {formatDate,formatMoney} from "@/lib/report-formatters";
export default function ReportSummary({report}){const s=report.summary,f=report.financials,c=report.metadata.currency;const items=[["Forecast covers",s.forecasted_covers],["Open days",s.open_days],["Peak day",s.peak_date?`${formatDate(s.peak_date)} (${s.peak_covers})`:"Unavailable"],["Average daily covers",s.average_daily_covers??"Unavailable"],["Staff assignments",s.total_staff_assignments],["Estimated labour cost",formatMoney(f.estimated_labour_cost,c)]];return <section className="report-section"><h2>Management summary</h2><div className="report-card-grid">{items.map(([label,value])=><article className="report-metric" key={label}><span>{label}</span><strong>{value}</strong></article>)}</div></section>}
