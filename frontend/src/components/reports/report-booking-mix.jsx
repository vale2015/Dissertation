export default function ReportBookingMix({reference}){const values=[["Same-day average",reference.same_day_average],["Walk-in average",reference.walk_in_average],["Advance booking average",reference.advance_booking_average],["Average booking duration",reference.average_booking_duration]];return <section className="report-section"><h2>Historical booking mix reference</h2><p className="report-note">These are recent model-input averages, not predicted booking totals.</p><div className="report-card-grid">{values.map(([label,value])=><article className="report-metric" key={label}><span>{label}</span><strong>{value??"Unavailable"}</strong></article>)}</div></section>}
