import {formatDate,formatMoney,formatWeather,formatRoles} from "@/lib/report-formatters";
export default function ReportDailyTable({report}){return <section className="report-section"><h2>Daily operational breakdown</h2><div className="report-table-scroll"><table className="report-table"><thead><tr><th scope="col">Date</th><th scope="col">Status</th><th scope="col">Covers</th><th scope="col">Staffing</th><th scope="col">Labour</th><th scope="col">Weather</th><th scope="col">Events</th><th scope="col">Warnings</th></tr></thead><tbody>{report.daily_breakdown.map(row=><tr className={row.status==="Closed"?"is-closed":""} key={row.date}><th scope="row">{formatDate(row.date)}<small>{row.day_of_week}</small></th><td>{row.status}</td><td>{row.predicted_covers}</td><td title={formatRoles(row.staffing.roles)}>{row.staffing.total_staff_assignments}</td><td>{formatMoney(row.estimated_labour_cost,report.metadata.currency)}</td><td>{formatWeather(row.weather)}</td><td>{row.events.available?`${row.events.event_count} event${row.events.event_count===1?"":"s"}`:"Unavailable"}</td><td>{row.warnings.length?`${row.warnings.length} warning${row.warnings.length===1?"":"s"}`:"None"}</td></tr>)}</tbody></table></div></section>}
