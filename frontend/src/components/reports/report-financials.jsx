import {formatMoney,formatPercent} from "@/lib/report-formatters";
export default function ReportFinancials({report}){const f=report.financials,c=report.metadata.currency;const items=[["Estimated revenue",formatMoney(f.estimated_revenue,c)],["Food cost",formatMoney(f.estimated_food_cost,c)],["Gross profit before labour",formatMoney(f.estimated_gross_profit_before_labour,c)],["Labour cost",formatMoney(f.estimated_labour_cost,c)],["Contribution after food and labour",formatMoney(f.estimated_contribution_after_food_and_labour,c)],["Labour to revenue",formatPercent(f.labour_to_revenue_percentage)]];return <section className="report-section"><h2>Financial projections</h2><div className="report-card-grid">{items.map(([label,value])=><article className="report-metric" key={label}><span>{label}</span><strong>{value}</strong></article>)}</div><p className="report-note">Forecast estimates for management planning, not an accounting statement.</p></section>}
